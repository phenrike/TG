CREATE DATABASE nsmainp;

USE nsmainp;

CREATE TABLE USUARIO
(
ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
LOGIN VARCHAR(100) NOT NULL UNIQUE, 
SENHA VARCHAR(100) NOT NULL, 
NOME VARCHAR(200) NOT NULL, 
SEXO VARCHAR(10),
FACE VARCHAR(100),
FACEpublic BOOLEAN,
WPP VARCHAR(100),
WPPpublic BOOLEAN,
INSTA VARCHAR(100),
INSTApublic BOOLEAN,
SNAP VARCHAR(100),  
SNAPpublic BOOLEAN,
TWITTER VARCHAR(100),
TWITTERpublic BOOLEAN,
EMAIL VARCHAR(200),
EMAILpublic BOOLEAN,
LINK VARCHAR(200),
LINKpublic BOOLEAN,
DTINSCRICAO DATETIME(6)
);
FOTO BLOB, #Para o protótipo ainda não irei usar foto no perfil(Fica para o acabamento do app)

CREATE TABLE COMPARTILHAMENTO
(
ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT, 
EMISSOR INT NOT NULL,
RECEPTOR INT NOT NULL,
DATAEHORA DATETIME,
FOREIGN KEY (EMISSOR)
        REFERENCES USUARIO(ID)
        ON DELETE CASCADE,
FOREIGN KEY (RECEPTOR)
        REFERENCES USUARIO(ID)
        ON DELETE CASCADE
);

INSERT INTO USUARIO VALUES(0,'psantos','psantos','Paulo Henrique Santos','M','paulohenrique.santos.5891',
			'TRUE','+5512982315156','TRUE','@_phenrike','TRUE',
			'ihenrikepaulo','TRUE','@_phenrike','TRUE',
			'paulosantossjc@hotmail.com','TRUE','www.pirateofvalley.tumblr.com','TRUE','2016-03-05');

SELECT NOME,SEXO,LOGIN,SENHA,FACE,WPP,INSTA,SNAP,TWITTER,EMAIL,LINK FROM USER;